# 🚀 Запуск без Docker

## Проблема
Docker Desktop не запущен или не установлен.

## ✅ Решение: Запуск без Docker

Приложение может работать **без Docker** в режиме локальной разработки:
- Использует **In-Memory базу данных** вместо PostgreSQL
- Использует **локальное хранилище** (`wwwroot/uploads`) вместо Yandex Object Storage

### Шаг 1: Запустить приложение

```powershell
# Перейти в папку проекта
cd C:\Users\konuh\source\repos\WebApplication27

# Запустить приложение
dotnet run
```

### Шаг 2: Открыть в браузере

Приложение будет доступно по адресу:
- **HTTPS:** https://localhost:5001
- **HTTP:** http://localhost:5000

### Что будет работать:

✅ Загрузка изображений (сохраняются в `wwwroot/uploads`)
✅ Просмотр галереи
✅ Удаление изображений
✅ Все функции приложения

⚠️ **Важно:** Данные хранятся в памяти и будут потеряны при перезапуске приложения.

---

## 🐳 Если хотите использовать Docker

### Вариант 1: Запустить Docker Desktop

1. Найдите **Docker Desktop** в меню Пуск
2. Запустите приложение
3. Дождитесь, пока Docker запустится (иконка в трее станет зеленой)
4. Попробуйте снова:
   ```powershell
   docker-compose up -d
   ```

### Вариант 2: Установить Docker Desktop

Если Docker не установлен:

1. Скачайте Docker Desktop:
   https://www.docker.com/products/docker-desktop

2. Установите и перезагрузите компьютер

3. Запустите Docker Desktop

4. Проверьте установку:
   ```powershell
   docker --version
   docker ps
   ```

---

## 📋 Сравнение режимов

| Функция | Без Docker | С Docker |
|---------|-----------|----------|
| База данных | In-Memory | PostgreSQL |
| Хранилище | Локальное | Локальное/Yandex S3 |
| Данные сохраняются | ❌ Нет | ✅ Да |
| Требует установки | ❌ Нет | ✅ Docker |
| Скорость запуска | ⚡ Быстро | 🐢 Медленно |
| Для разработки | ✅ Отлично | ✅ Отлично |
| Для продакшена | ❌ Нет | ✅ Да |

---

## 🎯 Рекомендации

### Для разработки и тестирования:
```powershell
# Просто запустите без Docker
dotnet run
```

### Для полного тестирования с PostgreSQL:
```powershell
# Убедитесь, что Docker Desktop запущен
docker-compose up -d
```

### Для продакшена:
- Используйте Kubernetes
- См. [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 🔧 Устранение проблем

### "Docker Desktop is not running"

**Решение:**
1. Запустите Docker Desktop из меню Пуск
2. Дождитесь полного запуска (30-60 секунд)
3. Проверьте: `docker ps`

### "Cannot connect to Docker daemon"

**Решение:**
1. Перезапустите Docker Desktop
2. Или перезагрузите компьютер
3. Или используйте `dotnet run` без Docker

### Порт уже занят

**Решение:**
```powershell
# Найти процесс на порту 5000
netstat -ano | findstr :5000

# Убить процесс (замените PID)
taskkill /PID <PID> /F
```

---

## 📝 Примеры команд

### Запуск без Docker (рекомендуется для начала)
```powershell
dotnet run
```

### Запуск с Docker Compose
```powershell
# Запустить Docker Desktop сначала!
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить
docker-compose down
```

### Запуск только приложения в Docker
```powershell
# Собрать образ
docker build -t imagegallery:latest .

# Запустить
docker run -p 8080:8080 imagegallery:latest
```

---

## ✨ Быстрый старт (без Docker)

Выполните эти команды для немедленного запуска:

```powershell
# 1. Перейти в папку проекта
cd C:\Users\konuh\source\repos\WebApplication27

# 2. Запустить приложение
dotnet run

# 3. Открыть в браузере
start https://localhost:5001
```

Готово! Приложение работает! 🎉

---

## 📚 Дополнительная информация

- [README.md](README.md) - Основная документация
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [FIX_NUGET.md](FIX_NUGET.md) - Проблемы с NuGet
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Устранение проблем

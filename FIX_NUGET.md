# 🔧 Исправление проблемы с NuGet

## Проблема
```
error NU1301: Не удалось загрузить индекс службы для источника https://api.nuget.org/v3/index.json.
The SSL connection could not be established
```

## ✅ Рекомендуемые решения (по порядку)

### 1. Использовать Visual Studio (САМЫЙ ПРОСТОЙ)

1. Откройте `WebApplication27.sln` в Visual Studio 2022
2. Щелкните правой кнопкой на Solution
3. Выберите **"Restore NuGet Packages"**
4. Дождитесь завершения

Visual Studio имеет более надежный механизм загрузки пакетов.

---

### 2. Проверить антивирус и файрвол

**Касперский, Avast, Norton и другие антивирусы могут блокировать SSL соединения NuGet.**

**Решение:**
1. Временно отключите антивирус
2. Попробуйте восстановить пакеты:
   ```powershell
   dotnet restore
   ```
3. Если помогло - добавьте исключение для `dotnet.exe` в антивирусе

**Путь к dotnet.exe:**
```
C:\Program Files\dotnet\dotnet.exe
```

---

### 3. Проверить прокси-сервер

Если вы в корпоративной сети:

```powershell
# Проверить текущие настройки прокси
netsh winhttp show proxy

# Если прокси настроен, добавьте его в nuget.config
```

Отредактируйте `nuget.config`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
  </packageSources>
  <config>
    <add key="http_proxy" value="http://proxy.company.com:8080" />
    <add key="http_proxy.user" value="domain\username" />
    <add key="http_proxy.password" value="password" />
  </config>
</configuration>
```

---

### 4. Сбросить сетевые настройки

```powershell
# Запустите PowerShell от имени администратора

# Сбросить прокси
netsh winhttp reset proxy

# Очистить DNS кэш
ipconfig /flushdns

# Сбросить Winsock
netsh winsock reset

# Перезагрузить компьютер
Restart-Computer
```

---

### 5. Использовать другой DNS

Попробуйте использовать Google DNS или Cloudflare DNS:

**Google DNS:**
- Основной: 8.8.8.8
- Дополнительный: 8.8.4.4

**Cloudflare DNS:**
- Основной: 1.1.1.1
- Дополнительный: 1.0.0.1

**Как изменить DNS:**
1. Панель управления → Сеть и Интернет → Центр управления сетями
2. Изменение параметров адаптера
3. Правой кнопкой на вашем подключении → Свойства
4. IPv4 → Свойства
5. Использовать следующие адреса DNS-серверов
6. Введите DNS адреса

---

### 6. Обновить .NET SDK

Возможно, проблема в старой версии SDK:

1. Проверьте версию:
   ```powershell
   dotnet --version
   ```

2. Скачайте последнюю версию .NET 8.0 SDK:
   https://dotnet.microsoft.com/download/dotnet/8.0

3. Установите и перезагрузите компьютер

---

### 7. Использовать мобильный интернет (временно)

Если у вас есть мобильный интернет:
1. Включите точку доступа на телефоне
2. Подключите компьютер к мобильному интернету
3. Попробуйте восстановить пакеты

Это поможет определить, проблема в вашей сети или нет.

---

### 8. Ручная загрузка пакетов (крайний случай)

Если ничего не помогает:

1. **Скачайте пакеты вручную:**
   - Перейдите на https://www.nuget.org
   - Найдите и скачайте каждый пакет:
     - AWSSDK.S3 (3.7.307.19)
     - Microsoft.EntityFrameworkCore (8.0.0)
     - Microsoft.EntityFrameworkCore.InMemory (8.0.0)
     - Npgsql.EntityFrameworkCore.PostgreSQL (8.0.0)
     - Microsoft.EntityFrameworkCore.Tools (8.0.0)

2. **Создайте локальный источник:**
   ```powershell
   # Создать папку
   mkdir C:\LocalNuGet
   
   # Скопировать все .nupkg файлы в C:\LocalNuGet
   ```

3. **Обновите nuget.config:**
   ```xml
   <?xml version="1.0" encoding="utf-8"?>
   <configuration>
     <packageSources>
       <clear />
       <add key="LocalPackages" value="C:\LocalNuGet" />
       <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
     </packageSources>
   </configuration>
   ```

4. **Восстановите пакеты:**
   ```powershell
   dotnet restore
   ```

---

### 9. Использовать Docker (обход проблемы)

Если пакеты не восстанавливаются, но Docker работает:

```powershell
# Собрать образ (Docker сам скачает пакеты внутри контейнера)
docker build -t imagegallery:latest .

# Запустить
docker run -p 8080:8080 imagegallery:latest
```

Docker использует собственную сеть и может обойти проблемы с вашей сетью.

---

## 🔍 Диагностика

Выполните эти команды для диагностики:

```powershell
# 1. Проверить версию .NET
dotnet --version

# 2. Проверить доступность NuGet
Test-NetConnection api.nuget.org -Port 443

# 3. Проверить DNS
nslookup api.nuget.org

# 4. Проверить прокси
netsh winhttp show proxy

# 5. Проверить источники NuGet
dotnet nuget list source

# 6. Очистить кэш
dotnet nuget locals all --clear

# 7. Попробовать восстановить с подробным выводом
dotnet restore --verbosity detailed
```

---

## 📞 Если ничего не помогло

1. **Используйте Visual Studio** - это самый надежный способ
2. **Проверьте интернет-соединение** - попробуйте другую сеть
3. **Обратитесь к системному администратору** - если в корпоративной сети
4. **Используйте Docker** - обход проблемы

---

## ✅ После решения проблемы

После успешного восстановления пакетов:

```powershell
# Собрать проект
dotnet build

# Запустить приложение
dotnet run
```

Приложение будет доступно по адресу: https://localhost:5001

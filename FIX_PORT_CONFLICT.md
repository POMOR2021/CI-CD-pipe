# 🔧 Исправление конфликта портов

## Проблема
```
Error: ports are not available: exposing port TCP 0.0.0.0:8080
bind: Only one usage of each socket address is normally permitted.
```

Порт 8080 уже занят другим процессом (в вашем случае - Java).

## ✅ Решения

### Вариант 1: Использовать другой порт (РЕКОМЕНДУЕТСЯ)

Я уже изменил `docker-compose.yml` - теперь приложение будет доступно на порту **8081**:

```powershell
# Запустить Docker Compose
docker-compose up -d

# Открыть в браузере
start http://localhost:8081
```

✅ **Готово!** Приложение теперь на порту 8081.

---

### Вариант 2: Остановить процесс на порту 8080

Если вы хотите использовать порт 8080:

#### Шаг 1: Найти процесс
```powershell
netstat -ano | findstr :8080
```

Вывод:
```
TCP    0.0.0.0:8080    LISTENING    6664
```

PID процесса: **6664** (Java)

#### Шаг 2: Остановить процесс
```powershell
# Остановить процесс (замените PID на ваш)
taskkill /PID 6664 /F
```

#### Шаг 3: Вернуть порт 8080 в docker-compose.yml

Если хотите использовать 8080, измените в `docker-compose.yml`:
```yaml
ports:
  - "8080:8080"  # Вместо 8081:8080
```

---

### Вариант 3: Проверить, что занимает порт

```powershell
# Найти процесс на порту
netstat -ano | findstr :8080

# Узнать имя процесса
tasklist /FI "PID eq <PID>"
```

Возможные процессы:
- **java.exe** - Java приложение
- **dotnet.exe** - .NET приложение
- **nginx.exe** - Nginx сервер
- **node.exe** - Node.js приложение
- **docker-proxy.exe** - Docker контейнер

---

## 🎯 Быстрое решение

### Если порт 8080 занят:

```powershell
# 1. Остановить все Docker контейнеры
docker-compose down

# 2. Запустить на порту 8081 (уже настроено)
docker-compose up -d

# 3. Открыть приложение
start http://localhost:8081
```

### Если хотите освободить порт 8080:

```powershell
# 1. Найти PID процесса
netstat -ano | findstr :8080

# 2. Остановить процесс (замените 6664 на ваш PID)
taskkill /PID 6664 /F

# 3. Изменить docker-compose.yml обратно на 8080
# ports:
#   - "8080:8080"

# 4. Запустить
docker-compose up -d
```

---

## 📋 Проверка доступных портов

### Проверить, какие порты заняты:

```powershell
# Все занятые порты
netstat -ano | findstr LISTENING

# Конкретный порт
netstat -ano | findstr :8080
netstat -ano | findstr :8081
netstat -ano | findstr :5000
```

### Найти свободный порт:

Попробуйте эти порты:
- 8081 ✅ (уже настроено)
- 8082
- 8083
- 3000
- 5000

---

## 🔄 Изменение порта в docker-compose.yml

Если хотите использовать другой порт:

```yaml
services:
  webapp:
    ports:
      - "ВНЕШНИЙ_ПОРТ:8080"  # Измените ВНЕШНИЙ_ПОРТ
```

Примеры:
```yaml
- "8081:8080"  # Приложение на localhost:8081
- "8082:8080"  # Приложение на localhost:8082
- "3000:8080"  # Приложение на localhost:3000
```

---

## 🐳 Docker команды

### Остановить все контейнеры:
```powershell
docker-compose down
```

### Остановить и удалить все:
```powershell
docker-compose down -v
```

### Проверить запущенные контейнеры:
```powershell
docker ps
```

### Остановить конкретный контейнер:
```powershell
docker stop <container_id>
```

---

## 🚀 Запуск без Docker

Если Docker создает проблемы, запустите приложение напрямую:

```powershell
# Запустить без Docker
dotnet run

# Приложение будет на:
# https://localhost:5001
# http://localhost:5000
```

См. [START_WITHOUT_DOCKER.md](START_WITHOUT_DOCKER.md) для подробностей.

---

## 📊 Текущая конфигурация

После моих изменений:

| Режим | Порт | URL |
|-------|------|-----|
| Docker Compose | 8081 | http://localhost:8081 |
| dotnet run (HTTPS) | 5001 | https://localhost:5001 |
| dotnet run (HTTP) | 5000 | http://localhost:5000 |

---

## ✨ Рекомендация

**Используйте порт 8081** - я уже настроил `docker-compose.yml`:

```powershell
docker-compose up -d
start http://localhost:8081
```

Готово! 🎉

---

## 🆘 Если ничего не помогает

1. **Перезагрузите компьютер** - освободит все порты
2. **Используйте `dotnet run`** - обходит проблему с портами
3. **Остановите ненужные сервисы** - освободит ресурсы

```powershell
# Самый простой способ
dotnet run
# Откройте: https://localhost:5001
```
